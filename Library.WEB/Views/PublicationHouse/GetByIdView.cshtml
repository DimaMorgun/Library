@model Library.ViewModelLayer.ViewModels.PublicationHouseViewModel

<div>
    <h4>Publication House</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Adress)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Adress)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Books)
        </dt>

        <dd>
            @foreach (var book in Model.Books)
            {
                @Html.Label(book.Name)
                <br />
            }
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Update", "UpdateView", new { id = Model.PublicationHouseId }) |
    @Html.ActionLink("Save To Json", "SaveToJSON", new { id = Model.PublicationHouseId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
